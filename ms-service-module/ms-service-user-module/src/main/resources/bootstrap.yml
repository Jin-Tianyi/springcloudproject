spring:
  cloud:
    config:
      #配置中心地址
      uri: http://localhost:4040
      #分支名
      lable: master
      #配置文件前缀名
      name: user
      #后缀名
      profile: dev
      #配置文件请求路径
      #http://localhost:4040/master/admin0-dev.yml


